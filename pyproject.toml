[project]
name = "workspace"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "dbt-core>=1.12.0",
    "dbt-snowflake>=1.12.0",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]